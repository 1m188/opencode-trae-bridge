## 1. 创建安装文档

- [x] 1.1 在仓库根目录创建 `install.md`，按 `specs/install-uninstall-docs/spec.md` 的规范编写：使用相对路径引用源文件，步骤描述保持抽象（"将文件 A 完整复制到位置 B"），覆盖从获取源文件到重启 opencode 的完整流程

## 2. 创建卸载文档

- [x] 2.1 在仓库根目录创建 `uninstall.md`，按 `specs/install-uninstall-docs/spec.md` 的规范编写：使用相对路径，步骤描述保持抽象，覆盖从删除部署文件到重启 opencode 的完整流程
